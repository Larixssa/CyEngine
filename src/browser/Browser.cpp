#include "Browser.h"

#include<string>

void Browser::openUrl(std::string url)
{
	if (!url.empty())
	{
		/* ... */
	}
}

void Browser::getUrlInfo(std::string url)
{
	if (!url.empty())
	{
		/* ... */
	}
}